# E2E environment (optional — defaults work for local docker/dev)
E2E_BASE_URL=http://localhost:3000
E2E_API_URL=http://localhost:5000/api
# Set to 1 if backend + frontend are already running
# E2E_SKIP_WEBSERVER=1
# E2E_PASSWORD=TestPass123!
# E2E_HEADED=1
