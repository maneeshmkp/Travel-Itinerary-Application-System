# Production defaults baked into `vite build` when Vercel env vars are missing.
# Prefer also setting these in Vercel → Project → Settings → Environment Variables.

VITE_API_URL=https://travel-itinerary-application-system.onrender.com/api
VITE_SOCKET_URL=https://travel-itinerary-application-system.onrender.com
